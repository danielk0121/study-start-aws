# 프로젝트 전역 작성 규칙

이 문서는 프로젝트의 모든 문서와 소스 코드에 적용하는 공통 규칙입니다.

## Markdown 문서

- Markdown 파일명은 한국어를 기본으로 사용합니다.
- Markdown 문서의 제목, 본문, 표, 설명은 한국어로 작성합니다.
- 기술 용어, AWS 서비스명, 명령어처럼 정확한 영문 표기가 필요한 경우에만 영문을 사용하고, 필요하면 한국어 설명을 함께 적습니다.
- 저장소 소개를 위해 관례적으로 사용하는 루트 `README.md`는 예외로 유지할 수 있습니다.

## 폴더

- 폴더명은 영문 소문자를 사용합니다.
- 여러 단어로 된 폴더명은 하이픈(`-`)으로 연결합니다.
- 예: `docs/`, `todo/`, `doing/`, `hold/`, `done/`, `results/`, `asserts/`

## 질문 문서와 식별 코드

- 질문 문서 한 파일에는 질문 5개를 작성합니다.
- 질문 문서는 `docs/todo/`, `docs/doing/`, `docs/hold/`, `docs/done/` 중 현재 상태의 폴더에 둡니다.
- 질문 문서 파일명은 `묶음번호-주제-질문.md` 형식을 사용합니다. 예: `001-AWS-기초-질문.md`
- 각 질문에는 `묶음번호-질문번호` 형식의 질문 코드를 부여합니다. 예: `001-01`
- 답변은 질문 문서와 분리해 `results/`에만 저장합니다.
- 답변 파일명은 `묶음번호-질문번호-주제.md` 형식을 사용합니다. 예: `001-01-IAM.md`
- 답변 파일의 첫 줄에는 답변할 질문 내용을 제목으로 작성합니다.
- `results/목차.md`에는 답변 파일의 링크를 기록합니다.

## 소스 코드

- 소스 코드 파일명, 디렉터리명, 식별자(변수·함수·클래스명)는 영문을 사용합니다.
- 소스 코드의 주석은 한국어로 작성합니다.
- 외부 라이브러리·API가 요구하는 표기나 언어 문법은 원래 규칙을 따릅니다.

## 커밋 메시지

- 커밋 메시지는 짧게 작성합니다.
- 변경한 파일 목록이나 파일별 수정 내용은 작성하지 않습니다.
- 작업 내용을 요약하는 한 문장만 작성합니다.
- 커밋 메시지는 한국어로 작성합니다. 단, 파일명과 고유명사 등은 필요한 경우 영문을 사용합니다.

## 적용 원칙

새 파일을 만들거나 기존 파일을 수정할 때 이 규칙을 먼저 확인합니다. 명확한 이유로 예외가 필요하면, 파일 안이나 관련 문서에 그 이유를 간단히 남깁니다.
